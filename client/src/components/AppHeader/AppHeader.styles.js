import { createStyles } from '@mantine/core';

export default createStyles(() => ({
  appHeaderWrapper: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    height: '100%',
    width: '100%',
  },

  appHeaderRightIcons: {
    display: 'flex',
    alignItems: 'center',
  },
}));
