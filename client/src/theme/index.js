import { COLOR_PALETTE } from './palette';

export const DEFAULT_THEME = {
  colors: COLOR_PALETTE,
  colorScheme: 'light',
  primaryColor: 'primary',
};

export { MyGlobalStyle } from './global-styles';
